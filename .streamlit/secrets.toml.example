# Copy to Streamlit Cloud → Settings → Secrets (paste as TOML)
# Do not commit real secrets to git.

API_BASE_URL = "https://YOUR-RAILWAY-API.up.railway.app"
API_KEY = "your-api-key-matching-railway"
